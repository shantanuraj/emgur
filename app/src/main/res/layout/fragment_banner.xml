<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/banner_fragment_background">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="@dimen/banner_large"
        android:elevation="@dimen/cardview_default_elevation"
        android:background="@color/banner_background"
        android:id="@+id/banner">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:fontFamily="sans-serif-light"
            android:textSize="@dimen/banner_text"
            android:paddingLeft="@dimen/text_margin"
            android:id="@+id/bannerText"
            android:layout_gravity="center_vertical"/>
    </LinearLayout>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/bannerImage"
        android:scaleType="centerCrop"
        android:src="@drawable/banner"
        android:layout_below="@+id/banner"
        android:layout_centerHorizontal="true"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:theme="@style/AppTheme.BannerButton"
        android:background="@color/material_grey_100"
        android:text="@string/login"
        android:id="@+id/loginButton"
        android:layout_above="@+id/skipButton"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        android:theme="@style/AppTheme.BannerButton"
        android:background="@color/material_grey_100"
        android:text="@string/skip"
        android:id="@+id/skipButton"
        android:layout_alignParentBottom="true"/>

</RelativeLayout>